# 官网巡检助手（第一期）

1. Chrome 打开 `chrome://extensions`，Edge 打开 `edge://extensions`。
2. 开启“开发者模式”。
3. 点击“加载已解压的扩展程序”，选择本目录。
4. 刷新求职进度本。
5. 在“投递记录 → 官网库”中找到公司并点击“检查”。
6. 官网打开后完成登录，确认页面已经显示投递记录，再点击浏览器工具栏中的扩展图标。
7. 点击“读取当前投递页面”，扩展会返回求职进度本并生成待确认结果。

扩展不会读取或保存密码、Cookie 和输入框值。页面可见文字只在扩展本地临时保存，成功交给求职进度本后立即删除。
