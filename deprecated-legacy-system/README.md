# 已废弃：旧版系统

此目录仅用于保存历史代码，不再维护，也不是当前部署入口。

- 状态：已废弃
- 技术栈：旧 Node.js 服务与静态前端
- 禁止将此目录用于新的生产部署
- 当前正式系统位于仓库根目录下的 `migration-poc/`

保留这些文件仅用于历史追溯、数据格式核对和紧急回滚参考。旧代码不得继续接收功能修改；所有新功能和修复均应在 `migration-poc/` 中完成。

本机旧版业务数据仍保留在仓库根目录的 `data/`，没有随代码移动，避免误操作用户数据。